import React from "react";
import ReactDOM from "react-dom/client";
import Trainingstagebuch from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <Trainingstagebuch />
    </React.StrictMode>
);
